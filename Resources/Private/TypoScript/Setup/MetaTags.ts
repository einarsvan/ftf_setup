##
# Meta tags setup
##

page.meta{

	# Set keywords and description to corresponding fields in the page properties
	description.data = levelfield:-1,description,slide
	keywords.data = levelfield:-1,keywords,slide

	# Deny indexing for now
	robots = noindex, nofollow
}
